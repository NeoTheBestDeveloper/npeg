#ifndef ALGHORITMS_H
#define ALGHORITMS_H

enum alghoritms { VERTICAL_MIRROR, HORIZONTAL_MIRROR, UNKNOWN_ALHORITM };

enum alghoritms get_alghoritm_type(const char *alghoritm);

#endif // OPTIONS_VALIDATOR_H
